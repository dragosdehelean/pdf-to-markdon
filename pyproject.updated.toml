[project]
name = "pdf-to-markdown"
version = "0.1.0"
description = "PDF → Markdown with maximum fidelity (PyMuPDF4LLM + explicit table extraction)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    # PyMuPDF4LLM (Markdown conversion) + extras:
    # - [layout] installs PyMuPDF Layout (better reading order, multi-column, etc.)
    # - [ocr] enables OCR assist (requires Tesseract installed on the system)
    "pymupdf4llm[ocr,layout]==0.2.7",

    # Explicit pin for compatibility with the above (and for Page.find_tables table extraction)
    "pymupdf==1.26.7",
]
